import { AgrandarLetraDirective } from './agrandar-letra.directive';

describe('AgrandarLetraDirective', () => {
  it('should create an instance', () => {
    // const directive = new AgrandarLetraDirective();
    // expect(directive).toBeTruthy();
  });
});
